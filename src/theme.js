import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#fff'
        },
        secondary: {
            main: '#192249'
        }

    },

    background: {
        default: 'red'
    }
})

export default theme;